# Lemma normalization map.
#
# Each non-comment line must contain exactly two tab-separated columns:
#
# source_lemma<TAB>normalized_lemma
#
# This file is intentionally empty. Add only mappings that have been
# verified against the configured NLP backend and the source corpus.
